# Day 04 — Conditional Statements in C++

## 📅 Day 04 Progress

Today I learned how C++ makes decisions using conditional statements.

The main concepts covered were:

- `if`
- `if-else`
- `else if`
- Nested `if`
- Boolean (`bool`) variables
- Logical AND `&&`
- Logical OR `||`
- Logical NOT `!`
- Combining conditions
- User input with `cin`
- Program execution flow
- Decision-making using real-world conditions

---

# 1. What Are Conditional Statements?

Conditional statements allow a program to make decisions.

A program can check a condition and decide which block of code should execute.

For example:

```cpp
int age = 20;

if (age >= 18)
{
    cout << "You are an adult";
}